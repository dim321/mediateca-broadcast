.flex.flex-col.gap-3.sm:flex-row.sm:items-center.sm:justify-between.mb-6
  h1.text-2xl.font-semibold.tracking-tight = title
  - if local_assigns[:actions].present?
    .flex.flex-wrap.items-center.gap-2 = actions
