<% content_for :title, t("admin.nav.broadcast_point_group_memberships") %>
<div class="mb-4 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
  <h1 class="text-2xl font-semibold text-gray-900"><%= t("admin.nav.broadcast_point_group_memberships") %></h1>
  <%= link_to t("admin.crud.new"), new_admin_broadcast_point_group_membership_path, class: admin_primary_button_class %>
</div>
<% if @broadcast_point_group_memberships.empty? %>
  <%= render "admin/shared/empty_state", title: t("admin.crud.empty.title"), description: t("admin.crud.empty.description"), action_path: new_admin_broadcast_point_group_membership_path, action_label: t("admin.crud.new") %>
<% else %>
  <div class="relative overflow-x-auto shadow-md sm:rounded-lg bg-white">
    <table class="w-full text-sm text-left text-gray-500">
      <thead class="text-xs text-gray-700 uppercase bg-gray-50">
        <tr>
          <th scope="col" class="px-6 py-3"><%= BroadcastPointGroup.model_name.human %></th>
          <th scope="col" class="px-6 py-3"><%= Screen.model_name.human %></th>
          <th scope="col" class="px-6 py-3"><span class="sr-only"><%= t("admin.chrome.actions") %></span></th>
        </tr>
      </thead>
      <tbody>
        <% @broadcast_point_group_memberships.each do |membership| %>
          <tr class="bg-white border-b hover:bg-gray-50">
            <td class="px-6 py-4"><%= admin_link_to_record(membership.broadcast_point_group) %></td>
            <td class="px-6 py-4"><%= admin_link_to_record(membership.screen) %></td>
            <td class="px-6 py-4 text-right whitespace-nowrap">
              <%= link_to t("admin.chrome.show"), admin_broadcast_point_group_membership_path(membership), class: "font-medium text-blue-700 hover:underline me-3" %>
              <%= link_to t("admin.chrome.edit"), edit_admin_broadcast_point_group_membership_path(membership), class: "font-medium text-blue-700 hover:underline" %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <div class="flex flex-col md:flex-row justify-between items-start md:items-center space-y-3 md:space-y-0 p-4">
      <span class="text-sm text-gray-500"><%= t("admin.chrome.page_entries", count: @broadcast_point_group_memberships.total_count) %></span>
      <%= paginate @broadcast_point_group_memberships, theme: "admin" %>
    </div>
  </div>
<% end %>
