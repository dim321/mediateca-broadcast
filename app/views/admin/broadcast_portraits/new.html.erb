<% content_for :title, t("admin.broadcast_portraits.new") %>

<div class="mb-4">
  <h1 class="text-2xl font-semibold text-gray-900"><%= t("admin.broadcast_portraits.new") %></h1>
</div>

<%= render "form", portrait: @portrait, rotations: @rotations, service_themes: @service_themes %>
