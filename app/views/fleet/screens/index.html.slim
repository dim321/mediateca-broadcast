- content_for :title, t('.title')

.mx-auto.w-full.max-w-6xl
  = render 'shared/page_header', title: t('.title')

  - if @screens.any?
    .grid.gap-3
      - @screens.each do |screen|
        .card.bg-base-100.border.border-base-300.shadow-sm
          .card-body.gap-1
            = link_to screen.name, fleet_screen_path(screen), class: 'link link-hover font-medium'
            p.text-sm.text-base-content/60 = screen.station.name
  - else
    .card.bg-base-100.border.border-base-300.shadow-sm
      .card-body
        p.text-base-content/60 = t('.empty')
